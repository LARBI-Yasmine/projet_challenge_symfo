{% extends 'base.html.twig' %}

{% block title %}Categorie
{% endblock %}

{% block body %}
	<h2 class="font-weight-normal healthh mt-5 ">La Categorie
		{{category.titre}}</h2>

	<div class="shadow p-3 mb-5 bg-body rounded col-5 position-absolute top-50 start-50 translate-middle">
		<div class="container col-10 mt-5 ">
			<div class="row">
				<table class="table">
					<tbody>

						<tr>
							<th>Titre</th>
							<td>{{ category.titre }}</td>
						</tr>
					</tbody>
				</table>
			</div>
			<table>
				<tr>
					<td>
						<a href="{{ path('app_category_index') }}" class="btn btn-primary">retour</a>

					</td>
					<td></td>
					<td>

						<a href="{{ path('app_category_edit', {'id': category.id}) }} " class="btn btn-warning">Modifier</a>
					</td>
					<td></td>

					<td>

						{{ include('Backend/Category/_delete_form.html.twig') }}
					</td>

				</tr>
			</table>
		</div>
	</div>
</div></div>{% endblock %}
